define([], function() {
    return function() {
    	/*
        localStorage.assign({
            host     : "wa111",
            channel  : window.siteNumber,
            siteName : this.model.spTitle2,
            operator : this.model.hdfsite_tab.split('_')[0]
        })*/
    };
});